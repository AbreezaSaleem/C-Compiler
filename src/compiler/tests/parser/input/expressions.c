int main(void) {
  char *is_string;
  int a, b, c, d, e, r, i, x, b9, t;
  a, b, c;

  a = b = c;

  a = b =c = d += e &= 45;

  a + b - c * d / e;

  --i;
  i++;

  a || b, &x, i++;

  !(a+(!(12 / 3)));

  a * b9 / 56 % t;
  d - i + 3;

  is_string = "Abreeza";

  return 0;
}
